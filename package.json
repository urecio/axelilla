{
  "version": "0.0.12",
  "engines": {
    "node": ">= 0.10.0"
  },
  "devDependencies": {
    "grunt": "~0.4.5",
    "grunt-bump": "^0.3.0",
    "load-grunt-tasks": "^3.1.0"
  },
  "scripts": {
    "bump": "git add --all; grunt bump;"
  }
}
